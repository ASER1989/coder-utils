# 打包工具：
    rollup
# 打包命令：
    rollup -c
# 发布命令：
   1、升级版本：npm version patch (需要先提交本地代码)
   2、发布：npm publish

# 备注：
    1、npm地址指向 registry=http://registry.npmjs.org/   否则会报错
    2、npm登陆：npm login
    3、npm登出：npm logout
    
   